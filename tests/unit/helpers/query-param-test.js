import {
  queryParam
} from 'googlanime/helpers/query-param';

module('QueryParamHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = queryParam(42);
  ok(result);
});
